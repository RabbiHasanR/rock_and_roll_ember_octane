import Route from '@ember/routing/route';
import { service } from '@ember/service';

export default class BandsRoute extends Route {
  // @service catalog;
  // @service apiData;

  // async model() {
  //   // let bands = this.catalog.fetchAll('bands');
  //   // return bands;
  //   console.log('bands:', this.catalog.bands);
  //   return this.catalog;
  // }
}
